#include <stdio.h>

int variable = 0;
int main(){
    printf("El valor de la variable es: %i\n",variable);

    //Le cambiamos el valor e incrementamos
    for (variable; variable<=20; variable+=2){
        continue;
    printf("\n Variable = %i",variable);

    variable = 10 + 5;

    printf("El valor de la variable es = %i",variable);
    }
    return 0;
    

}